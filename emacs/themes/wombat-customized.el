


;; (deftheme wombat-customized
;;   "Created 2015-03-31.")

;; (custom-theme-set-variables
;;  'wombat-customized
;;  ;; '(c-syntactic-indentation t)
;;  ;; '(c-tab-always-indent t)
;;  ;; '(company-auto-complete (quote (quote company-explicit-action-p)))
;;  ;; '(compilation-always-kill t)
;;  ;; '(compilation-auto-jump-to-first-error t)
;;  ;; '(compilation-environment nil)
;;  ;; '(compilation-search-path (quote (nil "/usr/bin/" "/opt/local/bin/")))
;;  ;; '(current-language-environment "UTF-8")
;;  ;; '(doxymacs-command-character "\\")
;;  ;; '(explicit-bash-args (quote ("--noediting" "-i")))
;;  ;; '(explicit-shell-file-name nil)
;;  ;; '(flymake-gui-warnings-enabled nil)
;;  ;; '(ispell-dictionary "deutsch")
;;  ;; '(ispell-extra-args nil)
;;  ;; '(ispell-program-name "/usr/bin/aspell")
;;  ;; '(load-home-init-file t)
;;  ;; '(mark-even-if-inactive t)
;;  ;; '(projectile-git-command "true")
;;  ;; '(projectile-project-root-files (quote ("rebar.config" "project.clj" "pom.xml" "build.sbt" "build.gradle" "Gemfile" "requirements.txt" "package.json" "gulpfile.js" "Gruntfile.js" "bower.json" "composer.json" "SConstruct")))
;;  ;; '(projectile-project-root-files-top-down-recurring (quote ("CVS" "SConscript")))
;;  ;; '(ps-paper-type (quote a4))
;;  ;; '(ps-print-color-p nil)
;;  ;; '(recentf-initialize-file-name-history t)
;;  ;; '(recentf-max-saved-items 8)
;;  ;; '(safe-local-variable-values (quote ((emacs-lisp-docstring-fill-column . 75))))
;;  ;; '(scroll-bar-mode (quote right))
;;  ;; '(semantic-c-dependency-system-include-path (quote ("/usr/include")))
;;  ;; '(semantic-decoration-styles (quote (("semantic-decoration-on-includes" . t) ("semantic-decoration-on-protected-members" . t) ("semantic-decoration-on-private-members" . t) ("semantic-tag-boundary" . t))))
;;  ;; '(semantic-idle-scheduler-idle-time 1)
;;  ;; '(semantic-idle-scheduler-work-idle-time 60)
;;  ;; '(user-full-name "Lukas Schuller")
;;  ;; '(visible-cursor nil)
;;  ;; '(void-text-area-pointer (quote arrow))
;;  ;; '(transient-mark-mode 1)
;;  '(show-paren-mode t)
;;  '(recentf-mode t)
;;  '(delete-selection-mode nil)
;;  '(blink-cursor-mode nil)
;;  '(compilation-scroll-output (quote first-error))
;;  '(compilation-skip-threshold 2))

;; (custom-theme-set-faces
;;  'wombat
;;  '(cursor ((t (:background "gray80"))))
;;  '(ido-first-match ((t (:weight extra-bold))))
;;  '(ido-only-match ((t (:foreground "ForestGreen" :weight bold))))
;;  '(ido-subdir ((t (:inherit font-lock-warning-face))))
;;  '(error ((t (:foreground "firebrick2" :weight bold))))
;;  '(compilation-info ((t (:foreground "#cae682")))))

;; (provide-theme 'wombat-customized)
